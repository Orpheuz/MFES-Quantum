class Square is subclass of Piece

operations
		public Square: Player ==> Square
		Square(p) == Piece(p);

		public canMoveTo: Tile * Tile ==> bool
		canMoveTo(ti, tf) == (
				return abs(ti.getCoordinates().x - tf.getCoordinates().x) = 1 or 
							 abs(ti.getCoordinates().y - tf.getCoordinates().y) = 1;
		);

end Square